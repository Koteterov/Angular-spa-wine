import { WineInfoDirective } from './wine-info.directive';

describe('WineInfoDirective', () => {
  it('should create an instance', () => {
    // const directive = new WineInfoDirective();
    // expect(directive).toBeTruthy();
  });
});
